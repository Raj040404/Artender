<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>
    <div class="login-container">
        <h2>Login</h2>
        
        <!-- ✅ Error Message Box -->
        {{#if error}}
        <div class="error-box" id="errorBox">
            {{error}}
        </div>
        {{/if}}

        <form class="login-form" method="post" action="/login">
            <div class="form-group">
                <label for="loginType">Login via:</label>
                <select id="loginType" name="loginType">
                    <option value="email">Email</option>
                    <option value="username">Username</option>
                </select>
            </div>

            <div class="form-group">
                <label for="loginInput">Enter Email or Username:</label>
                <input type="text" id="loginInput" name="loginInput" required autocomplete="off" />
            </div>

            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required autocomplete="off" />
            </div>

            <div class="form-group">
                <input type="submit" value="Login">
            </div>

            <div class="form-group">
                <p align="center">Don't have an account? <a href="/signup">Register</a></p>
            </div>
        </form>
    </div>

    <script>
        // ✅ Hide error message after 3 seconds
        document.addEventListener("DOMContentLoaded", function () {
            const errorBox = document.getElementById("errorBox");
            if (errorBox) {
                setTimeout(() => {
                    errorBox.style.opacity = "0";
                    setTimeout(() => errorBox.style.display = "none", 500);
                }, 3000);
            }
        });
    </script>
</body>
</html>
